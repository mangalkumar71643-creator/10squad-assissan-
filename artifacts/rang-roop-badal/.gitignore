# Native projects generated by `expo prebuild` — regenerated on demand,
# not checked in for a managed Expo workflow.
/android
/ios

# Release signing key and credentials — never commit these.
/secrets
