{
  "buildCommand": "cd ../../ && pnpm install --frozen-lockfile && pnpm --filter @workspace/game-lobby run build",
  "outputDirectory": "artifacts/game-lobby/dist",
  "env": {
    "PORT": "5000"
  }
}
